{
    "binary": 0,
    "filename": 6,
    "matrixsize": 1000,
    "timeout": 5,
    "seed": 10000141,
    "description": "large matrix multiplication 1 (smaller than ${matrixsize} * ${matrixsize}, ${timeout} sec timeout)",
    "preparation": "cat ${filename} > /dev/null"
}
