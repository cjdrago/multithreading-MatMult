{
    "binary": 0,
    "filename": 1,
    "matrixsize": 10,
    "timeout": 1,
    "seed": 10000019,
    "description": "toy matrix multiplication (smaller than ${matrixsize} * ${matrixsize}, ${timeout} sec timeout)",
    "preparation": "cat ${filename} > /dev/null"
}
