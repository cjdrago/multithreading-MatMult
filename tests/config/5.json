{
    "binary": 0,
    "filename": 5,
    "matrixsize": 800,
    "timeout": 1,
    "seed": 10000139,
    "description": "small matrix multiplication 4 (smaller than ${matrixsize} * ${matrixsize}, ${timeout} sec timeout)",
    "preparation": "cat ${filename} > /dev/null"
}
